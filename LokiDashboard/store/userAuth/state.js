export default ()=>({
  /**
   * Loged in user object.
   */
  user: null, //represents logged in user.

  /**
   * Logged in user permisions.
   */
  userPermissions: [],

  /**
   * Loading state for auth  pages.
   */
  isLoading: false
})